{% extends 'base.html.twig' %}
{% block title  %}Registration{% endblock %}
{% block body %}
{{form_start(form)}}
{{form_widget(form)}}
    <div class="form-group">
        <p><button type="submit" class="btn btn-success" >inscription</button></p>
    </div>
{{form_end(form)}}
{%endblock%}
    
   











